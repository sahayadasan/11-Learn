<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Learn</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if !@is_logged_in %>
        <li class="nav-item">
          <a class="nav-link" href="/users/sign_in"><i class="fa fa-recycle"></i> Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/users/sign_up"><i class="fa fa-plus-circle"></i> Register</a>
        </li>
      <% elsif @is_teacher %>
        <li class="nav-item">
          <a class="nav-link" href="/users"><i class="fa fa-users"></i> Users</a>
        </li>
      <% elsif @is_student %>
      <% end %>

      <% if @is_logged_in %>
        <li class="nav-item">
          <a class="nav-link" href="/users/edit"><i class="fa fa-id-card"></i> Profile</a>
        </li>
        <li class="nav-item">
          <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link' do %>
            <i class="fa fa-window-close"></i> <%= "#{@user.email} (#{@user.role})" %>
          <% end %>
        </li>
      <% end %>
  </div>
</nav>
